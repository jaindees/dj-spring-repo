newProperty: This is Changed now DJ!!!
oldProp: New DJ Property!!!
server:
  port: ${PORT:9099}

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/
